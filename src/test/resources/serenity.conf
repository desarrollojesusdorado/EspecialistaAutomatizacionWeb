serenity {
  tag.failures = "true"
  report {
    accessibility = true
    durations = "1,2,4,8,15,30,60"
    hide.empty.requirements = true
    exclude.tags = "resetappstate,singlebrowser,manual"
  }
}

headless.mode = false

webdriver {
  driver = chrome
  driver.autodownload=true
  driver_capabilities.common.unexpectedAlertBehaviour=ignore
  capabilities {
    browserName = "chrome"
    "goog:chromeOptions" {
      args = ["start-maximized","test-type", "ignore-certificate-errors",
        "incognito", "disable-infobars", "disable-gpu", "disable-default-apps", "disable-popup-blocking", "remote-allow-origins=*"]
    }
  }
}
# d# r# i# v# e# r# s#  # {#
#  #  # w# i# n# d# o# w# s#  # {#
#  #  #  #  # w# e# b# d# r# i# v# e# r# .# c# h# r# o# m# e# .# d# r# i# v# e# r#  # =#  # s# r# c# /# t# e# s# t# /# r# e# s# o# u# r# c# e# s# /# d# r# i# v# e# r# /# c# h# r# o# m# e# d# r# i# v# e# r# .# e# x# e#
#  #  # }#
# }
environments {
default {
   url.restful = "https://restful-booker.herokuapp.com"
}
}
